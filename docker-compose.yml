services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tradeedge
      POSTGRES_PASSWORD: tradeedge
      POSTGRES_DB: tradeedge
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
